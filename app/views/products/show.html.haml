%p
  %b Title:
  = product.title
%p
  %b Description:
  = product.description
%p
  %b Price:
  = product.price
%p
  %b Category
  = product.category.name
= link_to 'Edit', edit_category_product_path(category.id, product.id), class: 'btn btn-primary'

%h3 Reviews
= render 'reviews/form'

= render reviews
%br
= link_to 'Back', category_path(product.category), class: 'btn btn-primary'
